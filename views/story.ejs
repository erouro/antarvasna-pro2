<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= story.title %> - Antarvasna</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>body{font-family:'Inter',sans-serif;background:#fff;color:#000;}</style>
</head>
<body>
  <!-- Header/nav same -->
  <header class="bg-red-600 text-white p-4 text-center"><h1>Antarvasna</h1></header>

  <main class="p-4 max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold"><%= story.title %></h1>
    <p>On <%= story.date %> | <%= story.views %> views</p>
    <% story.categories.forEach(cat => { %><span class="text-red-600"><%= cat %></span> <% }); %>
    <div class="mt-4"><%= story.content %></div>
    <a href="/" class="text-red-600 mt-4 block">← Back to Home</a>
  </main>

  <!-- Footer/modals -->
</body>
</html>
